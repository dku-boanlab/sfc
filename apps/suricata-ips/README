Suricata (IPS mode)

# Installation

$ ./setup.sh  

$ sudo vi /etc/sudoers  
	Add the following line at the end of the file
	[YourID] ALL=NOPASSWD:ALL

$ sudo vi /etc/network/interfaces
	auto ens4
	iface ens4 inet manual

	auto ens5
	iface ens5 inet manual

$ sudo reboot  

# Execution

./start.sh (normal mode)  
./start.sh b (daemon mode)  
./stop.sh
